<%= stylesheet_link_tag "main" %>
<div class="header">
	<ul class="nav pull-right">
	  <li><%= link_to "Home",       root_path %></li>
	  <% if user_signed_in? %>
	  <li><%= link_to "Profile", edit_user_registration_path %></li>
	  <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
	  <% else %>
	    <li><%= link_to "Sign in", new_user_session_path %></li>
	  <% end %>
	</ul>
</div>
<br><br>

<h1>
	Üdv 
	<% if user_signed_in? %>
		<%= current_user.username %>
  	<% else %>
    	és csatlakozz te is
  	<% end %>
</h1>

<p>Find me in app/views/welcome/index.html.erb</p>

<table>
<tr>
	<td colspan="2">
		<br>
		<%= link_to "Új borklub létrehozásához kattint ide!", new_wine_club_path %>
		<br><br>
	</td>
</tr>
<tr>
	<td colspan="2">
		Borklubjaim listája<br><br>

		<table>
			<thead>
				<tr>
					<td>Borklub id</td>
					<td>Borklub neve</td>
				</tr>
			</thead>
			<tbody>
				<% @usersWineClubs.each do |p| %>
				<% p.addresses.each do |wineClubs| %>
				<tr>
					<td><%= wineClubs.id %></td>
					<td><%= wineClubs.name %></td>
				</tr>
				<% end %>
				<% end %>
			</tbody>
		</table>
	</td>
</tr>

</table>
